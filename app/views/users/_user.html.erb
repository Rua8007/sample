<li>
	<%= gravatar_for user%>
	<%= link_to user.name, user %>
	<% if current_user.try(:admin?) && !current_user?(user) %>
		<span>|</span> <%= link_to "delete", user_path(user) , method: :delete, data: { confirm: "You sure?" } %>
<% end %>
</li>